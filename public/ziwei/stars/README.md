# 紫微星曜人格海报

- 运行时只用 `{星名}.webp`
- 同目录 `{星名}.png` 仅作原图归档，**代码不引用**
- 已出图：
  - 十四主星
  - 六吉 / 六煞
  - 辅曜重点（禄存、天马）
  - 四化（化禄 / 化权 / 化科 / 化忌）
  - 十二宫封面（`命宫`…`父母`；`奴仆宫` 复用 `仆役`）
  - `MINOR_STAR_LORE` 全套
  - `SHENSHA_LORE` 全套（`飞廉` 复用 `蜚廉` 图）
- 无独立文件名时图鉴走字标；别名见 `star-art.ts` 的 `ART_ALIASES` / `PALACE_ART_ALIASES`
