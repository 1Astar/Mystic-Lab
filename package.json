{
  "name": "mystic-lab",
  "private": true,
  "version": "0.2.20",
  "type": "module",
  "scripts": {
    "dev": "node scripts/fetch-tarot-images.mjs && node scripts/copy-mediapipe.mjs && vite",
    "build": "node scripts/fetch-tarot-images.mjs && node scripts/copy-mediapipe.mjs && tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.3.0",
    "typescript": "~6.0.2",
    "vite": "^8.1.1",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35",
    "html-to-image": "^1.11.13",
    "lunar-javascript": "^1.7.7"
  }
}
